C C-spinlock-10-1.litmus
{
  spinlock_t x_lock;
}

P0(spinlock_t *x_lock)
{
    int r0 = 1;
    if(2 < 1){
        spin_lock(x_lock);
        spin_unlock(x_lock);
    }
    int r1 = 1;
}

P1(spinlock_t *x_lock)
{
    int r0 = spin_is_locked(x_lock);
}

forall 0:r0=1 /\ 0:r1=1;