C C-spinlock-05-1.litmus
{
  atomic_t x = 0;
  spinlock_t x_lock = 1;
}

P0(intptr_t *x, spinlock_t *x_lock)
{
    spin_lock(x_lock);
    int r0 = READ_ONCE(*x);
    WRITE_ONCE(*x, r0 + 1);
    spin_unlock(x_lock);
}

exists x=0